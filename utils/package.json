{
  "name": "@yumm/utils",
  "version": "1.0.1",
  "description": "utils component of yumm",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish"
  },
  "author": "fatiudeen",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/winston": "^2.4.4",
    "crypto": "^1.0.1",
    "del-cli": "^5.1.0",
    "typescript": "^5.2.2"
  }
}
