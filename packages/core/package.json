{
  "name": "@yumm/core",
  "version": "1.0.0",
  "description": "core component of yumm",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish"
  },
  "author": "fatiudeen",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@yumm/helpers": "^1.0.3"
  }
}
