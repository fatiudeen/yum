{
  "name": "@yumm/utils",
  "version": "1.0.6",
  "description": "utils component of yumm",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "del ./dist/*",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"$npm_config_m\" && npm version patch && npm run build && npm publish"
  },
  "author": "fatiudeen",
  "license": "MIT",
  "devDependencies": {
    "@types/winston": "^2.4.4",
    "crypto": "^1.0.1",
    "del-cli": "^5.1.0",
    "typescript": "^5.2.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
